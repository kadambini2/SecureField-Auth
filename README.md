# SecureField-Auth
SecureField Auth is an offline edge AI biometric system for Datalake 3.0. It delivers secure facial recognition (>95% accuracy) and active liveness checks (blink tracking) on mid-range phones without internet. Features a &lt;5MB footprint, &lt;1s latency, and an encrypted local-DB architecture with automated AWS cloud sync and absolute data purging.
